import styled from 'styled-components';

export const Container = styled.button`
  width: 250px;
  height: 45px;

  color: #fff;
  background: #ff4c00;

  border: none;
  border-radius: 4px;

  font-weight: 500;
  font-size: 17px;

  transition: 0.2s;
  &:hover {
    background: #e34400;
  }
`;
